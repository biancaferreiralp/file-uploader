<%= link_to 'Back', root_path %>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th>address</th>
    </tr>
  </thead>

  <tbody>
    <% @merchants.each do |m| %>
      <tr>
        <td><%= m.name %></td>
        <td><%= m.address %></td>
        <td><%= link_to 'Detalhes', merchant_path(m.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
